<Window x:Class="WpfApp.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local ="clr-namespace:WpfApp"
        Title="Учет сотрудников" Height="450" Width="800">
    <Window.Resources>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <DataTemplate x:Key="EmployeeCardTemplate">
            <Border BorderBrush="LightGray" BorderThickness="1" Padding="10" Margin="5">
                <StackPanel>
                    <TextBlock Text="{Binding FullName}" FontWeight="Bold"/>
                    <TextBlock Text="{Binding Position}" FontStyle="Italic"/>
                    <TextBlock Text="{Binding Department}"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ProgressBar Grid.Row="0" Height="10" Margin="0,0,0,5" 
                        IsIndeterminate="{Binding IsLoading}"
                        Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>

            <Menu Grid.Row="1">
                <MenuItem Header="Файл">
                    <MenuItem Header="Сохранить" Command="{Binding SaveDataCommand}"/>
                    <MenuItem Header="Выход" Command="{Binding CloseCommand}"/>
                </MenuItem>
                <MenuItem Header="Сотрудники">
                    <MenuItem Header="Добавить" Command="{Binding AddEmployeeCommand}"/>
                    <MenuItem Header="Редактировать" Command="{Binding EditEmployeeCommand}"/>
                    <MenuItem Header="Удалить" Command="{Binding DeleteEmployeeCommand}"/>
                </MenuItem>
            </Menu>

            <GroupBox Header="Фильтр по отделам" Grid.Row="2">
                <StackPanel Orientation="Horizontal" Margin="5">
                    <RadioButton Content="Все" GroupName="Filter" IsChecked="True" Command="{Binding FilterCommand}" CommandParameter="Все"/>
                    <RadioButton Content="HR" GroupName="Filter" Command="{Binding FilterCommand}" CommandParameter="HR"/>
                    <RadioButton Content="IT" GroupName="Filter" Command="{Binding FilterCommand}" CommandParameter="IT"/>
                    <RadioButton Content="Финансы" GroupName="Filter" Command="{Binding FilterCommand}" CommandParameter="Финансы"/>
                    <RadioButton Content="Маркетинг" GroupName="Filter" Command="{Binding FilterCommand}" CommandParameter="Маркетинг"/>
                </StackPanel>
            </GroupBox>

            <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
                <ListBox ItemsSource="{Binding Employees}"
                        SelectedItem="{Binding SelectedEmployee}"
                        ItemTemplate="{StaticResource EmployeeCardTemplate}"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        Margin="0,10">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                </ListBox>
            </ScrollViewer>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Добавить" Command="{Binding AddEmployeeCommand}" Margin="5"/>
                <Button Content="Редактировать" Command="{Binding EditEmployeeCommand}" Margin="5" Width="100"/>
                <Button Content="Удалить" Command="{Binding DeleteEmployeeCommand}" Margin="5" Width="80"/>
            </StackPanel>
        </Grid>

        <!-- Чат -->
        <Grid Grid.Column="1" Margin="10,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ListBox Grid.Row="0" ItemsSource="{Binding ChatMessages}"/>
            <TextBox Grid.Row="1" Margin="0,5" Text="{Binding ChatMessage, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Grid.Row="2" Content="Отправить" Margin="0,5" Command="{Binding SendChatMessageCommand}"/>
        </Grid>
    </Grid>
</Window>